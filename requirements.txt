# INC_Trade — Broker Module Dependencies
# Clean Architecture: domain (zero deps) → infrastructure → adapters

# Async HTTP
httpx>=0.27.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-benchmark>=5.0.0
# VCR.py cassette replay for offline replay of recorded broker HTTP traffic.
# Used by brokers/tests/conftest.py to intercept `requests.Session` calls
# inside ``@pytest.mark.live`` tests when FORCE_MARKET_OPEN is unset.
pytest-recording>=0.13.0
# PyYAML — runtime dep of both vcrpy's cassette serialiser and the
# scripts/verify_cassettes.py secrets audit. The audit parses cassettes
# via ``yaml.safe_load``; without this dep the script fails on import.
pyyaml>=6.0

# Data
pydantic>=2.0.0
pandas>=2.0.0
